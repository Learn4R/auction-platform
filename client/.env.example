# Base URL of the deployed server (REST API + Socket.io). Baked into the
# build at build time — set this in your hosting platform's environment
# variables before running the build. Defaults to http://localhost:3000
# for local development if not set.
VITE_API_URL="https://your-server-domain.example.com"
